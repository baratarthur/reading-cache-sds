uses libs.network.rpc.RPCUtil

data NamespacedAddress {
    char namespace[]
    Address address
}

interface AddressHandler {
    void addAddressesInNamespace(store Address a[], char namespace[])
    void removeAllFrom(char namespace[])
    Address[] getAddresses(char namespace[])
}