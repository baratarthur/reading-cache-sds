uses time.DateUtil
uses libs.krabs.MetricsStore

data Post {
    int id
    int userId
    int likes
    char content[]
    DateTime ttl // Time to live in miliseconds, for cache eviction
}

interface PostsRepository {
    void addPost(store Post newPost, RequestMetric rm)
    Post getPostById(store int postId, RequestMetric rm)
    Post[] getUserFeed(store int userId, RequestMetric rm)
}
